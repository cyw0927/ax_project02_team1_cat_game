# C-01~C-10 사용자 사고 시나리오 추적표

| 항목·화면/정상 흐름 | 대표 Worst Case·원인 | 서버 감지·방어 | UI·DB/state·다음 단계·테스트/TBD |
|---|---|---|---|
| C-01 방 목록 | 목록은 WAITING이나 클릭 순간 시작 | 참가 시 서버 상태 재검증 | 오래된 방 제거/재조회; write 없음 |
| C-02 방 생성 | 생성 버튼 매크로로 방 여러 개 | 요청 식별/기존 활성 방 정책 `TBD` | 생성 중 잠금은 보조; 중복 생성 경쟁 테스트 |
| C-03 입장 | 중복 참가/응답 유실 | `(room,user)` unique/기존 참가 반환 | 기존 대기실 복원 |
| C-04 마지막 자리 | 두 명이 같은 마지막 자리를 봄 | ROOM row만 짧게 `FOR UPDATE`, 상태·정원 재검증 | 한 명만 성공; lock 중 네트워크 대기 금지 |
| C-05 Ready | 연결 끊긴 Ready가 남음 | 참가·방 상태·connection 상태 재동기화 | 서버 snapshot 표시; 취소 정책 `TBD` |
| C-06 방장 시작 | 시작과 마지막 입장이 경쟁 | 같은 짧은 room lock에서 시작 조건 판정 | 실패 사유 표시; 최소 인원/조건 `TBD` |
| C-07 비방장 시작 | 클라이언트 역할 위조 | 인증 user와 room owner 비교 | 403; ROOM 상태 불변 |
| C-08 정답/점수 | 중복 채점 결과로 점수 두 번 증가 | attempt contribution unique/조건부 반영 | 서버 점수만 표시; 점수식 `TBD` |
| C-09 오답 | 시스템 오류를 오답/감점 처리 | 채점 상태 분류 | 오답과 시스템 오류 분리; 감점 규칙 `TBD` |
| C-10 종료 | 마지막 점수와 종료가 경쟁·응답 유실 | 종료 상태 단방향 전이, 남은 작업 정책 `TBD` | 결과 재조회/보상 멱등; 승자·동점·AFK·보상 `TBD` |

WebSocket은 제안 후보일 뿐 확정하지 않는다. reconnect는 놓친 이벤트 재생만 믿지 않고 서버 snapshot으로 복구한다.

