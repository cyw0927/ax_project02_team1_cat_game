# B-01~B-10 사용자 사고 시나리오 추적표

가격·단회/다회·할인·확률·픽업·천장·중복/mileage는 전부 `TBD`다.

| 항목·화면/정상 흐름 | 대표 Worst Case·원인 | 서버 감지·방어 | UI·DB/state·다음 단계·테스트/TBD |
|---|---|---|---|
| B-01 화면 진입 | 잔액/풀 조회 일부 실패를 정상 화면으로 오인 | 각 조회 성공·활성 설정 검증 | 뽑기 비활성/재시도; 실제 수치 임의 표시 금지 |
| B-02 요청 단위 처리 | 버튼 매크로로 의도치 않은 다중 차감 | request_id·서버 비용·조건부 차감 | 요청별 처리 중 표시; 비용/단회 존재 `TBD` |
| B-03 다회 후보 | 다회 응답 일부만 지급 | 지원 여부부터 검증; 하나의 논리 거래로 전부 성공/rollback | 부분 결과 금지; 다회 기능·개수·할인 `TBD` |
| B-04 부족 | 오래된 잔액으로 가능 표시 | 조건부 원자 UPDATE 영향 row 확인 | 최신 잔액과 부족 안내; 결과 생성 없음 |
| B-05 연타/동시 요청 | 두 기기에서 동시에 이전 잔액 사용 | request별 멱등성·원자 차감 | 재시도는 기존 결과; 동시 요청 테스트 |
| B-06 신규 결과 | 동시 요청이 둘 다 신규로 판단 | USER_CATS 정책/unique는 중복 정책 확정 후 결정 | 실제 소유 결과 표시; 중복 보유 정책 `TBD` |
| B-07 희귀도 결과 | 클라이언트가 결과/확률 위조 | 서버 풀·활성 후보·설정 검증 | 서버 결과만 표시; 확률표·희귀도 규칙 `TBD` |
| B-08 중복 | ERD mileage 존재만으로 자동 변환 | 정책 설정 없으면 변환 금지 | 중복 처리 선택지를 확정값처럼 표시 금지; 전환/수치 `TBD` |
| B-09 중간 오류 | 차감 성공 후 결과/지급 실패 | 차감·결과 기록·지급을 transaction/복구 식별자로 연결 | 돈만 빠진 성공 화면 금지; rollback/복구 테스트 |
| B-10 천장 후보 | 재시도 때 카운터/랜덤 재굴림 | result history·request_id 구조는 신규 ERD `TBD` | 애니메이션과 실제 지급 분리; 천장 존재/기준 `TBD` |

앱 크래시·응답 유실 시 애니메이션을 다시 추첨하지 않고 서버에 확정된 동일 결과를 복원한다.

